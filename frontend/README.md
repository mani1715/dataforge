# Production Deployment

**Development URLs:**
- Frontend: http://localhost:3000
- Backend: http://localhost:8001/api

**api.js baseURL:** `'http://localhost:8001/api'` (direct - proxy bypassed for reliability)

**npm run build → static serve:** Update baseURL to your production backend.
